 {
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
   "scripts": {
    "start": "node server.js",
    "build": "cd client && npm install && npm run build",
    "deploy": "gh-pages -d build"
  }
}
